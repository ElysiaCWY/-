# 简历管理（打包版）使用说明

本文面向 **已拿到安装包或可执行文件** 的用户，说明如何安装、配置与日常使用。开发者构建方式见仓库根目录 `README.md`。

---

## 一、你拿到的可能是哪种文件

| 类型 | 说明 |
|------|------|
| **`resume-manager_0.1.0_x64-setup.exe`**（NSIS） | 常见「下一步」安装向导，按提示选安装目录即可。 |
| **`resume-manager_0.1.0_x64_en-US.msi`**（MSI） | 企业或静默安装常用；安装后同样在选定目录生成程序文件。 |
| **`resume-manager.exe` + 附属文件** | 绿色解压版：请保持安装程序解压出的目录结构完整（尤其 **`resources`** 文件夹），不要只拷贝单个 exe。 |

安装或解压后，安装根目录下通常包含：

- `resume-manager.exe`：主程序（双击启动；**正式打包版启动时不会额外弹出黑色命令行窗口**）。
- `resources/`：内置资源目录，其中带有 **`app-config.json` 默认模板**（见下文「配置文件」）。
- 其他由安装器生成的文件（如卸载入口等），以你本机为准。

---

## 二、运行环境要求

- **操作系统**：Windows 10 / 11（与当前安装包目标一致，x64）。
- **大语言模型（解析简历、JD 筛选等必备）**：本程序通过 HTTP 调用模型服务，**模型本身不在安装包内**。请至少满足以下一种：
  - **Ollama**（推荐入门）：本机安装 [Ollama](https://ollama.com/)，并在命令行执行 `ollama pull <模型名>`，与配置里的 `modelPath` 一致（内置默认示例为 `qwen2.5:3b`，可按需修改配置）。
  - **LM Studio**：本机安装 LM Studio，开启本地 OpenAI 兼容服务，在配置中将 `llmProvider` 设为 `lmstudio`，`llamaCliPath` 填服务地址（可留空使用默认 `http://127.0.0.1:1234/v1`），`modelPath` 填界面中的模型 ID。
  - **DeepSeek / 阿里云 DashScope（兼容模式）**：在配置中设置对应 `llmProvider`，填写 `modelPath` 与 `llmApiKey`（或使用官方说明的环境变量，如 `DEEPSEEK_API_KEY`、`DASHSCOPE_API_KEY`）。具体字段以你使用的 `app-config.json` 为准。

若未安装或未启动模型服务，导入解析、JD 计算匹配分等功能会报错或无法完成。

---

## 三、配置文件 `app-config.json`

程序按以下 **优先级** 查找配置（找到即用）：

1. **与 `resume-manager.exe` 同目录** 的 `app-config.json`（最高优先级，适合你自己改密钥、改模型名）。
2. **`<安装目录>\resources\app-config.json`**：安装包内置的默认模板（一般为 Ollama 示例，**不含你的个人 API Key**）。
3. 开发/特殊布局时，还可能沿 exe 向上层目录或「当前工作目录」查找；一般用户只需关心前两步。

**「项目根目录」**（程序在此自动创建 `data/`、`parsed-results/`、`logs/` 等）：

- 若你使用 **exe 同目录** 的 `app-config.json`，项目根即为 **该 exe 所在目录**。
- 若仅使用内置的 **`resources\app-config.json`**，项目根仍为 **安装根目录**（与 exe 同级），数据不会写到 `resources` 里面。

**若安装在 `C:\Program Files\...`**：普通用户可能 **无法写入** 安装目录下的配置或数据。如遇保存失败、无法生成 `data` 等，请将程序安装到 **有写权限的目录**（例如 `D:\Apps\简历管理\`），或在 exe 旁放置自己的 `app-config.json` 并确保该目录可写。

配置为 JSON，主要字段含义（名称区分大小写，与文件内一致）：

| 字段 | 说明 |
|------|------|
| `llmProvider` | 后端类型，如 `ollama`、`lmstudio`、`deepseek`、`dashscope` / `qwen` 等。 |
| `llamaCliPath` | Ollama 服务根地址，或 LM Studio / 云厂商的 OpenAI 兼容 **Base URL**（部分可留空走默认）。 |
| `modelPath` | 模型名或云端模型 ID（与所选服务商文档一致）。 |
| `llmApiKey` | 云端 API 密钥（可留空若已设置环境变量）。 |
| `threads` | 线程数等（依后端实际支持而定）。 |
| `temperature` | 采样温度。 |

修改配置后 **重启程序** 生效。

---

## 四、数据保存在哪里

| 内容 | 位置 |
|------|------|
| **解析结果库、按岗位归档的 JSON、运行日志** | 项目根下：`data\`（含 `resumes.db`）、`parsed-results\`、`logs\app.log` 等。 |
| **简历库列表、JD 列表** | 系统目录：`%LOCALAPPDATA%\resume-manager\`（与是否「绿色版」无关）。 |

备份建议：若重装系统或换机，请同时备份 **项目根目录** 与上述 `%LOCALAPPDATA%\resume-manager\`（如需保留简历库与 JD 记录）。

---

## 五、主要功能与操作提示

### 1. 简历导入与解析

- 支持 **`.pdf`、`.docx`**。
- **不支持** 旧版 **`.doc`**（需用 Word 等另存为 `.docx`）；图片型简历无 OCR 时无法直接解析。

批量导入后使用界面上的解析按钮；解析过程会写日志到 **`logs\app.log`**（无控制台时也可打开该文件排查）。

### 2. 简历库与 JD

- 在「简历库」中可浏览、筛选、删除已入库简历（删除会联动清理解析索引与相关落盘文件，具体逻辑见 `README.md`）。
- 在「JD 管理 / 智能筛选」中填写岗位、粘贴 JD，使用 **「计算匹配分」** 做结构化预筛与精排（依赖本地模型，耗时与候选人数、模型速度有关；界面会显示进度）。

### 3. 导出（含「主题简历」PDF）

- **标准简历 PDF**：在 JD 页等入口导出时，程序会调用 `export_resume_pdf_from_json` 等逻辑。
- **默认打包版（仅 exe + `resources`，项目根下没有 Node 依赖）**  
  - **可以正常导出 PDF**，走的是程序内置的 **纯文本排版 PDF**（`printpdf`），**不依赖** `resume-cli`，也 **不是** 浏览器主题那种版式。
- **主题版（JSON Resume + 本地主题 `jsonresume-theme-local`）**  
  - **可以**，但需你在 **项目根目录**（与 `resume-manager.exe` 同级、即 `data` / `parsed-results` 所在目录）先具备 **`resume-cli`**：
    - 存在 **`node_modules\.bin\resume.cmd`**（由 `npm install` 安装 `resume-cli` 等产生）。
  - 检测到上述 CLI 后，程序会视情况 **自动释放** 内置的 `jsonresume-theme-local`（若目录里还没有主题文件）；再调用  
    `resume export ... --theme ./jsonresume-theme-local --format pdf`。  
  - 主题链路通常还依赖 **Node.js** 与 `resume-cli` 拉取的 **Puppeteer / Chromium** 等，体积与首次下载较大，需本机网络与写权限。
- **如何准备主题导出环境（与仓库内脚本一致）**  
  - 将仓库中的 **`package.json`**、**`package-lock.json`**（建议）与 **`start-theme-export.bat` / `start-theme-export.ps1`** 复制到 **项目根**，在该目录执行脚本或手动 `npm install`，再启动程序导出 PDF。  
  - 仅复制 exe **没有** `package.json` 时，`npm install` 无法完成，主题导出不会启用。

- **Word → PDF**（若你使用的版本包含该页）：依赖本机安装 **Microsoft Word 桌面版**；调用 Word 时可能短暂出现相关进程窗口，属正常现象。

### 4. 日志与排错

- **文件日志**：`<项目根>\logs\app.log`。
- 若需从命令行启动并查看 Rust 侧控制台日志，可在 PowerShell 中设置 `RUST_LOG` 后启动 exe（一般仅排错使用），例如：  
  `$env:RUST_LOG="resume_manager=debug"; & "D:\路径\resume-manager.exe"`

---

## 六、卸载与更新

- **安装版**：使用「应用和功能」中的卸载项，或 MSI/NSIS 提供的卸载方式。卸载前请自行备份上文的「项目根」与 `%LOCALAPPDATA%\resume-manager\`。
- **更新程序**：安装新版本到同一目录或新目录后，建议先备份 `data`、`parsed-results` 再覆盖；配置以你保留的 `app-config.json` 为准。

---

## 七、常见问题

1. **提示找不到配置或未找到 `app-config.json`**  
   确认 `resources` 文件夹与安装程序一同存在；或在与 exe 同级手动新建/复制一份 `app-config.json`。

2. **解析或 JD 报错与网络/模型相关**  
   检查 Ollama 是否已启动、`ollama list` 是否包含配置的模型；云端接口检查 Key 与 Base URL。

3. **安装目录无法写入**  
   更换到有写权限的路径安装，或使用 exe 旁的自定义 `app-config.json` 且保证项目根可写。

4. **杀毒软件拦截**  
   部分环境可能对未签名 exe 报警；请从可信来源获取安装包，必要时加入白名单。

5. **导出 PDF 不是「网页主题」样式**  
   说明未在项目根执行过 `npm install`（没有 `node_modules\.bin\resume.cmd`），程序已自动使用内置纯文本 PDF。若需要 JSON Resume 主题版，请按上文「导出」一节准备 Node + `resume-cli`。

---

（以下为分发进阶说明；一般用户仅用内置 PDF 即可。）
