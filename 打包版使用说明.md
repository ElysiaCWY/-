# 简历管理（打包版）使用说明

本文档用于发送给最终使用者。按本文步骤操作，可在 Windows 上直接使用应用。

## 一、前置需求（必须）

1. **操作系统**：Windows 10 / 11（64 位）
2. **本地模型服务**：已安装并可运行 [Ollama](https://ollama.com/)
3. **模型已拉取**：需包含配置文件中的模型  
   当前默认模型：`qwenpaw-flash:2b-q4_k_m`

安装命令（PowerShell）：

```powershell
ollama create qwenpaw-flash:2b-q4_k_m -f Modelfile
ollama run qwenpaw-flash:2b-q4_k_m
```

## 二、收到文件后如何放置

请将以下文件放在**同一目录**：

- `resume-manager.exe`
- `app-config.json`

然后直接双击 `resume-manager.exe` 启动。

## 三、首次使用建议

1. 打开 `app-config.json`，确认配置是否可用：
   - `llamaCliPath`：通常保持 `http://127.0.0.1:11434`
   - `modelPath`：确认是本机已 `pull` 的模型名
   - `threads`、`temperature`：可按机器性能调整
2. 先导入 1-2 份简历做测试，确认解析成功后再批量导入。

## 四、功能前置依赖说明

- **简历解析 / JD 筛选**：依赖 Ollama + 对应模型（必须）
- **Word 转 PDF**：除上述外，还需要本机安装 **Microsoft Word（桌面版）**
- **标准简历 PDF 导出**：无 Node 环境时会自动使用内置 PDF 方案

## 五、常见问题

### 1）启动后提示无法连接模型

请检查：

- Ollama 是否已启动
- `app-config.json` 的 `llamaCliPath` 是否正确
- `modelPath` 对应模型是否已 `ollama pull`

### 2）只发了 exe，为什么不能正常用？

`app-config.json` 是必需文件，请与 `resume-manager.exe` 一起提供。

### 3）日志在哪里？

程序运行后会在当前项目根目录生成：

- `logs/app.log`

可用于排查解析和导出问题。

## 六、分发方建议（给发送者）

打包发送时，推荐压缩为一个 zip：

- `resume-manager.exe`
- `app-config.json`
- `打包版使用说明.md`（本文档）

这样接收方解压后按说明即可直接使用。
